<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.ssafy.vue.model.mapper.CommentMapper">

	<!-- articleno에 따른 댓글 조회 -->
	<select id="list" resultType="commentDto">
		select memono, userid, comment, memotime, articleno
		from memo
		where articleno = #{articleno}
		order by memotime desc
	</select>

	<!-- 댓글 등록 -->
	<insert id="create" parameterType="commentDto">
		insert into memo (memono, userid, comment, memotime, articleno)
		values (#{memono}, #{userid}, #{comment}, now(), #{articleno})
	</insert>

	<!-- 댓글 수정 -->
	<update id="modify" parameterType="commentDto">
		<![CDATA[
		update memo
		set comment = #{comment}
		where memono = #{memono}
		]]>
	</update>

	<!-- articleno에 따른 댓글 삭제 -->
	<delete id="delete" parameterType="int">
		delete
		from memo
		where memono = #{memono}
	</delete>
</mapper>