<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.vue.model.mapper.HouseMapMapper">

<!-- select * from baseaddress b, guguncode g where g.gugunCode = 4111000000 and b.gugunName = g.gugunName; -->

	<select id="getBaseAddressByCode" parameterType="string" resultType="sidoGugunCodeDto">
		select sidoName, gugunName, dongName, dongCode
		from baseaddress
		where dongCode = #{dongCode}
	</select>
	
	<select id="getNameAndlatLngByGugunCode"  parameterType="string" resultType="sidoGugunCodeDto">
		select s.sidoName, g.gugunName, b.lat, b.lng
		from sidocode s, guguncode g, baseaddress b
		where left(s.sidoCode, 2) = #{sidoCode}
		and left(g.gugunCode, 5) = #{gugunCode}
		limit 0, 1;
	</select>
<!-- 		select s.sidoCode, s.sidoName, g.gugunCode, g.gugunName, b.lat, b.lng, 
		from sidocode s, guguncode g, baseaddress b
		where left(s.sidoCode, 2) = #{sidoCode}
		and left(g.gugunCode, 5) = #{gugunCode}
		limit 0, 1 -->

	<!-- <select id="getLatLngByName" parameterType="string" resultType="sidoGugunCodeDto">
		select lat, lng
		from baseaddress 
		where gugunName like concat('%', #{gugunName}, '%')
		and sidoName like concat('%', #{sidoName}, '%')
	</select>

	<select id="getSidoBySidoCode" parameterType="string" resultType="sidoGugunCodeDto">
		select sidoName
		from sidocode
		where left(sidoCode, 2) = #{sidoCode}
	</select>
	
	<select id="getGugunByGugunCode" parameterType="string" resultType="sidoGugunCodeDto">
		select gugunName
		from guguncode
		where left(gugunCode, 5) = #{gugunCode}
	</select> -->

	<select id="getSido" resultType="sidoGugunCodeDto">
		select left(sidoCode,2) sidoCode, sidoName
		from sidocode
		order by sidoCode
	</select>
	
	<select id="getGugunInSido" parameterType="string" resultType="sidoGugunCodeDto">
		select left(gugunCode,5) gugunCode, gugunName
		from guguncode
		where left(gugunCode,2) = #{sido}
		order by gugunCode
	</select>
	
	<select id="getDongInGugun" parameterType="string" resultType="houseInfoDto">
		select distinct dongName, dongCode
		from baseaddress
		where left(dongCode, 5) = #{gugun}
		order by dongName
	</select>
	
	<select id="getAptInDong" parameterType="string" resultType="houseInfoDto">
		select h.aptCode, h.aptName, h.buildyear, h.dongCode, h.dongName, h.jibun, h.lat, h.lng, si.sidoname, gu.gugunname,
				(select dealAmount from housedeal where aptCode = h.aptCode and no = (select max(no) from housedeal where aptCode = h.aptCode)) recentPrice
		from houseinfo h
		left join sidocode si
		on left(h.dongcode,2) = left(si.sidocode,2)
		left join guguncode gu
		on left(h.dongcode,5) = left(gu.guguncode,5)
		where dongCode = #{dong} 
		order by aptName
	</select>
	
</mapper>